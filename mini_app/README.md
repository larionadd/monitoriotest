# Monitorio Mini App

Static Telegram Mini App for the Monitorio bot.

Publish this folder with GitHub Pages, then set:

```env
MINI_APP_URL=https://USERNAME.github.io/REPOSITORY/
```

The Mini App sends actions to the bot via `Telegram.WebApp.sendData`.
